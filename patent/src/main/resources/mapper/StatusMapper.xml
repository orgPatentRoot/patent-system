<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.suixingpay.patent.mapper.StatusMapper" >
  <resultMap id="StatusResultMap" type="com.suixingpay.patent.pojo.Status" >
    <id column="status_id" property="statusId" jdbcType="INTEGER" />
    <result column="status_name" property="statusName" jdbcType="VARCHAR" />
  </resultMap>

  <select id="queryAllStatus" resultMap="StatusResultMap">
   select status_name,status_id from status where status_id between -1 and 12
  </select>

  <select id="selectStatusNameById" resultMap="StatusResultMap">
     select status_name from status where status_id = #{statusId}
  </select>

  <select id="selectStatusIdByName" resultMap="StatusResultMap">
  select status_id from status where status_name = #{statusName}
  </select>

  <select id="queryAllStatusAfter" resultMap="StatusResultMap">
    select status_id,status_name from status where status_id between 6 and 12
  </select>

  <select id="selectAfterByYml" resultMap="StatusResultMap">
    select status_id,status_name from status where status_id &gt; #{patentStatusId}
  </select>
<!--&gt; #{patentNeedCheckStatus}-->
  <select id="selectBeforeByYml" resultMap="StatusResultMap">
    select status_id,status_name from status where status_id between 2 and #{patentStatusId}
  </select>

</mapper>